<div class='col-md-8'>
    <h1><%= @topic.title %></h1>

    <%= paginate @blogs %>

    <%= render @blogs %>

    <%= paginate @blogs %>

    <div>
        <%= link_to "Back to blogs", blogs_path %>
    </div>
</div>
